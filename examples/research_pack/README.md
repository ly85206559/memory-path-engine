# Research Pack

Example domain pack for research notes and claim/evidence notebooks.

```bash
python -m memory_engine.demo --scenario research
```

Ingest uses `example_research_pack` (`research_pack` alias). Nodes are typed as `note` and may carry `cites`, `contradicts`, `exception_to`, and `depends_on` edges.
